# Patch notes - MMO Skill Taming Pack

Per-version public release notes (newest first). Same paradigm as the main mod repo: YAML frontmatter (`version`/`type: patch-note`/`status`), user-facing voice, no em-dashes. `CURSEFORGE.md` is the public listing; keep its Versions table in sync.

- [[1.2.1]] - held (requires plugin 1.6.1 + CommonLib 2.1.0): every quest carries a picture, and the repeating quests are badged off their own timing rather than hand-labelled.
- [[1.2.0]] - released (requires plugin 1.6.0 + CommonLib 2.0.0): quests and achievements move onto the shared quest/achievement engine (internal-only, no player-facing change).
- [[1.1.0]] - superseded, folded into 1.2.0 (written for plugin 1.3.0): boost rewards actually grant, localized reward lines.
